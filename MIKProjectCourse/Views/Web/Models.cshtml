@{
    ViewData["Title"] = "Model Basics in ASP.NET Core";
}

<div class="container models-page">
    <h1 class="text-center mb-4">Model Basics in ASP.NET Core</h1>

    <div class="card border-primary mb-4">
        <div class="card-header bg-primary text-white d-flex align-items-center">
            <i class="fas fa-database me-2"></i>
            <h5 class="mb-0">What Are Models?</h5>
        </div>
        <div class="card-body">
            <p>
                <strong>Models represent the DATA of your application.</strong>
            </p>
            <p>
                If your app was a real system:
            </p>
            <ul>
                <li><strong>Model</strong> = Data (Student, User, Product)</li>
                <li><strong>Controller</strong> = Logic (what happens)</li>
                <li><strong>View</strong> = UI (what the user sees)</li>
            </ul>
            <p class="mb-0">
                Models are written as <strong>normal C# classes</strong>.
            </p>
        </div>
    </div>

    <div class="card border-warning mb-4">
        <div class="card-header bg-warning text-dark">
            <i class="fas fa-exclamation-triangle me-2"></i>
            Before You Learn Models
        </div>
        <div class="card-body">
            <ul class="mb-0">
                <li>What a <strong>class</strong> is</li>
                <li>What <strong>properties</strong> are</li>
                <li>Basic data types: <code>int</code>, <code>string</code>, <code>bool</code></li>
            </ul>
        </div>
    </div>

    <h2 class="text-center mb-4">A Simple Model Example</h2>

    <div class="card mb-5">
        <div class="card-header bg-body-secondary fw-semibold">
            Student Model
        </div>
        <div class="card-body">
            <pre class="code-block">
<code>
public class Student
{
    public int Id { get; set; }
    public string Name { get; set; }
    public int Age { get; set; }
    public bool IsActive { get; set; }
}
</code>
            </pre>

            <p class="mt-3 mb-0">
                This model describes <strong>what data a Student has</strong>.
            </p>
        </div>
    </div>

    <h2 class="text-center mb-4">Why Models Matter</h2>

    <div class="card mb-5">
        <div class="card-body">
            <ul>
                <li>They define the <strong>structure</strong> of your data</li>
                <li>They are shared between Controllers and Views</li>
                <li>They make your code <strong>organized</strong></li>
                <li>They are required for databases (later)</li>
            </ul>
        </div>
    </div>

    <h2 class="text-center mb-4">Models in the MVC Flow</h2>

    <div class="row g-4 mb-5 text-center">
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <i class="fas fa-user fa-2x mb-2"></i>
                    <h6>User</h6>
                    <p>Requests a page</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <i class="fas fa-cogs fa-2x mb-2"></i>
                    <h6>Controller</h6>
                    <p>Creates / loads a Model</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <i class="fas fa-database fa-2x mb-2"></i>
                    <h6>Model</h6>
                    <p>Holds the data</p>
                </div>
            </div>
        </div>

    <h2 class="text-center mb-4">Using a Model in a Controller</h2>

    <div class="card mb-5">
        <div class="card-body">
            <pre class="code-block">
<code>
public IActionResult Details()
{
    var student = new Student
    {
        Id = 1,
        Name = "Ana",
        Age = 20,
        IsActive = true
    };

    return View(student);
}
</code>
            </pre>
        </div>
    </div>

    <h2 class="text-center mb-4">Using a Model in a View</h2>

    <div class="card mb-5">
        <div class="card-body">
            <pre class="code-block">
<code>
@@model Student

&lt;h2&gt;@@Model.Name&lt;/h2&gt;
&lt;p&gt;Age: @@Model.Age&lt;/p&gt;
</code>
            </pre>

            <p class="mb-0">
                Views <strong>display</strong> model data. They do NOT modify it.
            </p>
        </div>
    </div>

    <!-- DATA VALIDATION -->
    <h2 class="text-center mb-4">Model Validation (Very Important)</h2>

    <div class="card mb-5">
        <div class="card-body">
            <p>
                Models can define <strong>rules</strong> using Data Annotations.
            </p>

            <pre class="code-block">
<code>
using System.ComponentModel.DataAnnotations;

public class Student
{
    public int Id { get; set; }

    [Required]
    public string Name { get; set; }

    [Range(6, 100)]
    public int Age { get; set; }
}
</code>
            </pre>

            <p class="mb-0">
                These rules are used automatically by MVC.
            </p>
        </div>
    </div>

    <h2 class="text-center mb-4">Types of Models Beginners Should Know</h2>

    <div class="row g-4 mb-5">
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <strong>Domain Models</strong>
                    <p class="mb-0">Represent real data (Student, Course)</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <strong>View Models</strong>
                    <p class="mb-0">Data prepared for a View</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <strong>DTOs</strong>
                    <p class="mb-0">Data sent between layers / APIs</p>
                </div>
            </div>
        </div>
    </div>

    <h2 class="text-center mb-4">Models & Databases (Coming Next)</h2>

    <div class="card mb-5">
        <div class="card-body">
            <p>
                Later, models will be connected to databases using:
            </p>
            <ul class="mb-0">
                <li>Entity Framework Core</li>
                <li>SQL tables</li>
                <li>Migrations</li>
            </ul>
        </div>
    </div>

    <div class="card border-success mb-5">
        <div class="card-header bg-success text-white">
            Key Points to Remember
        </div>
        <div class="card-body">
            <ul class="mb-0">
                <li>Models are C# classes</li>
                <li>They define application data</li>
                <li>They can include validation</li>
                <li>They are shared between Controllers and Views</li>
            </ul>
        </div>
    </div>

    <div class="navigation-buttons">
        <a href="@Url.Action("Controllers", "Web")" class="btn btn-outline-primary">
            ← Controllers
        </a>
        <a href="@Url.Action("Views_", "Web")" class="btn btn-primary">
            Views →
        </a>
    </div>
</div>

<style>
.models-page {
    max-width: 900px;
    padding: 20px;
}

.code-block {
    background-color: var(--bs-body-bg);
    color: var(--bs-body-color);
    border: 1px solid var(--bs-border-color);
    border-radius: 6px;
    padding: 15px;
    overflow-x: auto;
}

.navigation-buttons {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    padding-top: 20px;
    border-top: 2px solid var(--bs-border-color);
}

.navigation-buttons .btn {
    padding: 12px 28px;
    font-weight: 500;
    border-radius: 6px;
}

@@media (max-width: 768px) {
    .navigation-buttons {
        flex-direction: column;
    }

    .navigation-buttons .btn {
        width: 100%;
        text-align: center;
    }
}
</style>
