@{
    ViewData["Title"] = "If Else";
}

<body data-topic="ifelse">
    <div class="container mt-4">
        <h1 class="mb-4">🔀 If - Else</h1>
        <p>Sometimes you want the computer to make decisions. That's where <strong>if</strong> and <strong>else</strong> come in!</p>
        <p>They let your program choose what to do based on a condition.</p>

        <p>Example: If your age is 13 or more, you are a teenager.</p>

        <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="languageToggle" onchange="convertCode()">
            <label class="form-check-label" for="languageToggle">Convert to C#</label>
        </div>

        <div class="code-container mb-3">
            <div class="code-header">Code Example</div>
            <div class="code-content">
<pre id="codeBlock">
int age = 14;
if (age >= 13) {
    cout << "You are a teenager!";
} else {
    cout << "You are not a teenager.";
}
</pre>
            </div>
        </div>

        <p>You can also use <strong>if</strong> by itself if you don’t need the “else”.</p>

        <div class="code-container mb-3">
            <div class="code-header">Code Example</div>
            <div class="code-content">
<pre>
if (age > 18) {
    cout << "You can vote!";
}
</pre>
            </div>
        </div>

        <p><strong>Conditions</strong> use symbols:</p>
        <ul>
            <li><code>==</code> equal to</li>
            <li><code>!=</code> not equal to</li>
            <li><code>&gt;</code> greater than</li>
            <li><code>&lt;</code> less than</li>
        </ul>

        <div class="navigation-buttons mt-4">
            <a href="@Url.Action("Variables", "SP")" class="btn btn-outline-primary">⬅ Variables</a>
            <a href="@Url.Action("ForLoops", "SP")" class="btn btn-primary">ForLoops →</a>
        </div>
    </div>

    <style>
        .container {
            max-width: 800px;
            padding: 20px;
            background-color: var(--bs-body-bg);
        }

        h1 {
            color: var(--bs-heading-color);
            margin-bottom: 20px;
        }

        p, li, code {
            color: var(--bs-body-color);
            line-height: 1.6;
        }

        ul {
            padding-left: 20px;
        }

        .code-container {
            margin: 20px 0;
            border: 1px solid var(--bs-border-color);
            border-radius: 5px;
            overflow: hidden;
            background-color: var(--code-bg);
        }

        .code-header {
            background-color: var(--code-header-bg);
            padding: 10px 15px;
            border-bottom: 1px solid var(--bs-border-color);
            font-family: 'SFMono-Regular', Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
            font-size: 0.9rem;
            color: var(--bs-body-color);
        }

        .code-content {
            padding: 15px;
            overflow-x: auto;
            font-family: 'SFMono-Regular', Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        }

        pre {
            margin: 0;
            white-space: pre-wrap;
            background-color: transparent;
        }

        .navigation-buttons {
            display: flex;
            justify-content: space-between;
        }

        .btn {
            padding: 8px 20px;
            border-radius: 4px;
            font-weight: 500;
            text-decoration: none;
        }

        .btn-primary {
            background-color: var(--bs-primary);
            border-color: var(--bs-primary);
            color: #fff;
        }

        .btn-outline-primary {
            color: var(--bs-primary);
            border-color: var(--bs-primary);
            background-color: transparent;
        }
    </style>

    <script>
        function convertCode() {
            const topic = document.body.getAttribute("data-topic");
            const isChecked = document.getElementById("languageToggle").checked;
            const codeBlock = document.getElementById("codeBlock");

            const cppCode = {
                ifelse: `int age = 14;
if (age >= 13) {
    cout << "You are a teenager!";
} else {
    cout << "You are not a teenager.";
}`
            };

            const csharpCode = {
                ifelse: `int age = 14;
if (age >= 13)
{
    Console.WriteLine("You are a teenager!");
}
else
{
    Console.WriteLine("You are not a teenager.");
}`
            };

            codeBlock.textContent = isChecked ? csharpCode[topic] : cppCode[topic];
        }
    </script>
</body>
