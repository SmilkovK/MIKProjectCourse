@{
    ViewData["Title"] = "For Loops";
}
<body data-topic="loops">
    <div class="container mt-4">
        <h2>🔁 For Loops</h2>
        <p>Loops help you repeat things without writing them many times.</p>
        <p>A <strong>for loop</strong> repeats a block of code as many times as you want.</p>

        <p>Example: Count from 1 to 5.</p>

        <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="languageToggle" onchange="convertCode()">
            <label class="form-check-label" for="languageToggle">Convert to C#</label>
        </div>

        <pre id="codeBlock" class="card p-3" style="background-color: var(--bs-tertiary-bg); color: var(--bs-body-color);">
for (int i = 1; i <= 5; i++) {
    cout << "Clap " << i << endl;
}
    </pre>

        <p>This loop says:</p>
        <ul>
            <li>Start with <code>i = 1</code></li>
            <li>Keep going while <code>i &lt;= 5</code></li>
            <li>Each time, add 1 to <code>i</code></li>
        </ul>

        <p>Another example: Print numbers from 10 to 1</p>

        <pre class="card p-3" style="background-color: var(--bs-tertiary-bg); color: var(--bs-body-color);">
for (int i = 10; i >= 1; i--) {
    cout << i << " ";
}
    </pre>

        <div class="d-flex justify-content-between mt-5">
            <a class="btn btn-secondary" asp-controller="SP" asp-action="IfElse">⬅ Back</a>
            <a class="btn btn-primary" asp-controller="SP" asp-action="Arrays">Next ➡</a>
        </div>

    <script>
            function convertCode() {
                const topic = document.body.getAttribute("data-topic");
                const isChecked = document.getElementById("languageToggle").checked;
                const codeBlock = document.getElementById("codeBlock");

                const cppCode = {
                    variables: `int age = 13;
        float height = 1.55;
        char grade = 'A';
        string name = "Alex";`,

                    ifelse: `int age = 14;
        if (age >= 13) {
            cout << "You are a teenager!";
        } else {
            cout << "You are not a teenager.";
        }`,

                    loops: `for (int i = 1; i <= 5; i++) {
            cout << "Clap " << i << endl;
        }`,

                    arrays: `int numbers[5] = {10, 20, 30, 40, 50};
        cout << numbers[0];`,

                    strings: `string fruits[3] = {"Apple", "Banana", "Cherry"};
        cout << fruits[0];`
                };

                const csharpCode = {
                    variables: `int age = 13;
        float height = 1.55f;
        char grade = 'A';
        string name = "Alex";`,

                    ifelse: `int age = 14;
        if (age >= 13)
        {
            Console.WriteLine("You are a teenager!");
        }
        else
        {
            Console.WriteLine("You are not a teenager.");
        }`,

                    loops: `for (int i = 1; i <= 5; i++)
        {
            Console.WriteLine("Clap " + i);
        }`,

                    arrays: `int[] numbers = {10, 20, 30, 40, 50};
        Console.WriteLine(numbers[0]);`,

                    strings: `string[] fruits = {"Apple", "Banana", "Cherry"};
        Console.WriteLine(fruits[0]);`
                };

                // Replace content
                codeBlock.textContent = isChecked ? csharpCode[topic] : cppCode[topic];
            }
    </script>




</body>
